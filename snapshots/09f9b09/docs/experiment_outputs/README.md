# Experiment Outputs

This folder stores persisted machine-readable outputs from experiment harnesses.

Conventions:

- use timestamp-first filenames
- keep one JSON artifact per run
- include a timestamp-first `title` field inside each manifest
- pair important runs with a human-readable trace in `docs/traces/`
